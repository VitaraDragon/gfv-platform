# Copia in functions/.secret.local (gitignored) — chiavi per sync secret + emulator.
# NON mettere queste chiavi in functions/.env (il deploy le carica come env normali → conflitto Secret Manager).
#
# Sincronizza su Firebase:
#   .\scripts\sync-functions-secrets.ps1 -Deploy
#   (oppure: npm run sync:functions-secrets)

GEMINI_API_KEY=
OPENWEATHER_API_KEY=
RESEND_API_KEY=
STRIPE_SECRET_KEY=
SENTRY_DSN=
