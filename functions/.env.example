# Copia in .env per emulator locale (non committare .env)
# In produzione usa: firebase functions:secrets:set RESEND_API_KEY
RESEND_API_KEY=
# OpenWeather One Call 3.0 — firebase functions:secrets:set OPENWEATHER_API_KEY
OPENWEATHER_API_KEY=
