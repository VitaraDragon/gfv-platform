# 🔌 Come Abilitare Maps JavaScript API

## 📋 Passo-Passo

### STEP 1: Vai alla Libreria API

1. Nella **Google Cloud Console**, nel menu laterale sinistro
2. Clicca su **"API e servizi"** (o **"APIs & Services"**)
3. Clicca su **"Libreria"** (o **"Library"**)

---

### STEP 2: Cerca Maps JavaScript API

1. Nella barra di ricerca in alto, digita: **"Maps JavaScript API"**
2. Clicca sul risultato **"Maps JavaScript API"** (dovrebbe essere il primo)

---

### STEP 3: Abilita l'API

1. Nella pagina che si apre, vedrai informazioni sull'API
2. In alto, clicca il pulsante **"ABILITA"** (o **"ENABLE"**)
3. Attendi qualche secondo (5-10 secondi)
4. Vedrai un messaggio di conferma che l'API è stata abilitata

---

### ✅ Verifica

Dopo aver abilitato, dovresti vedere:
- Il pulsante cambia da **"ABILITA"** a **"GESTISCI"** (o **"MANAGE"**)
- Un messaggio verde di conferma
- L'API appare nella lista delle API abilitate

---

## 🎯 Alternative: Se Non Vedi "Libreria"

Se non trovi "Libreria" nel menu:

1. Clicca su **"API e servizi"** → **"Dashboard"**
2. In alto, clicca **"+ ABILITA API E SERVIZI"** (o **"+ ENABLE APIS AND SERVICES"**)
3. Cerca **"Maps JavaScript API"**
4. Clicca su di essa
5. Clicca **"ABILITA"**

---

## 📍 Percorso Completo

```
Google Cloud Console
  → Menu laterale sinistro
    → "API e servizi" (APIs & Services)
      → "Libreria" (Library)
        → Cerca "Maps JavaScript API"
          → Clicca sul risultato
            → Clicca "ABILITA" (ENABLE)
```

---

## ⚠️ Nota Importante

**Geometry Library** non è un'API separata da abilitare. È inclusa automaticamente quando carichi Maps JavaScript API con il parametro `&libraries=geometry` nel codice (che abbiamo già fatto in `terreni-standalone.html`).

---

## ✅ Dopo Aver Abilitato

1. L'API è pronta per l'uso
2. La tua chiave API funzionerà correttamente
3. Puoi testare le mappe in `terreni-standalone.html`

---

**Se hai problemi, dimmi dove ti blocchi!** 🚀

